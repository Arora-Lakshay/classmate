const notificationsBtn = document.querySelector(".page.dashboard main .main-wrapper .utils .secondary .utility.notifications button#notify");

notificationsBtn.addEventListener("click", e => {
  location.href = "/in/notifications";
});